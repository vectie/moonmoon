// Generated by vectie/moonlib/scripts/generate-web-i18n.mjs.
const catalogs = {
  "en-US": {
    "common.refresh": "Refresh",
    "common.save": "Save",
    "common.cancel": "Cancel",
    "common.open": "Open",
    "common.close": "Close",
    "common.search": "Search",
    "common.settings": "Settings",
    "common.loading": "Loading",
    "common.ready": "Ready",
    "common.start": "Start",
    "common.stop": "Stop",
    "common.status": "Status",
    "common.error": "Error",
    "common.reset": "Reset",
    "moon.terrain": "Terrain",
    "moon.mission": "Mission",
    "moon.timeline": "Timeline",
    "moon.layers": "Layers",
    "moon.operator": "Operator",
    "moon.telemetry": "Telemetry",
    "moon.details": "Details",
    "moon.live": "Live",
    "moon.lunar_operator": "Lunar operator",
    "moon.globe_label": "Movable source-backed Moon globe",
    "moon.physical": "Physical",
    "moon.lighting_time": "Lighting timestamp",
    "moon.focus_site": "Focus site",
    "moon.pause_orbit": "Pause orbit",
    "moon.resume_orbit": "Resume orbit",
    "moon.evidence_authority": "Evidence authority",
    "moon.current_blockers": "Current blockers",
    "moon.evidence_dossier": "Evidence dossier",
    "moon.elevation": "Elevation",
    "moon.slope": "Slope",
    "moon.roughness": "Roughness",
    "moon.hazard": "Hazard",
    "moon.routes": "Routes",
    "moon.evidence": "Evidence",
    "moon.power": "Power",
    "moon.selected_cell": "Selected cell",
    "moon.blockers": "Blockers",
    "moon.route_preview": "Route preview",
    "moon.site_details": "Site details",
    "moon.no_selection": "No selection",
    "moon.warning": "Warning",
    "moon.site_terrain": "Site terrain",
    "moon.first_square": "First trusted square",
    "moon.selected_route": "Mission selected route",
    "moon.score": "Score",
    "moon.max_grade": "Max grade",
    "moon.ranked_window": "Ranked next window",
    "moon.compare_routes": "Compare route candidates",
    "moon.candidate": "Candidate",
    "moon.mission_selected": "Mission selected",
    "moon.site_score": "Site score",
    "moon.blocked_routes": "Blocked routes",
    "moon.energy_margin": "Energy margin Wh",
    "moon.motion_handoff": "Motion handoff",
    "moon.lunar_site_model": "Lunar site model",
    "moon.route_candidates": "Route candidates",
    "moon.blocked": "Blocked",
    "moon.confidence": "Confidence",
    "moon.source": "Source",
    "moon.motion_contract": "Motion contract",
    "moon.physics_core": "Physics core",
    "moon.motion_owner": "Motion owner",
    "moon.simulation": "Simulation",
    "moon.facts": "Facts",
    "moon.catalog_evidence": "Catalog evidence",
    "moon.catalog_validation": "Catalog validation",
    "moon.evidence_loaded": "Evidence loaded",
    "moon.reset_view": "Reset view",
    "moon.mission_decision": "Mission decision",
    "moon.next_action": "Next required action",
    "moon.controlling_blockers": "Controlling blockers",
    "moon.illumination_evidence": "Illumination evidence",
    "moon.illumination_block": "Illumination block",
    "moon.illumination_review": "Illumination review",
    "moon.illumination_pass": "Illumination pass",
    "moon.current_route_window": "Current route window",
    "moon.evidence_handoff": "Evidence and handoff",
    "moon.not_viable": "Not viable"
  },
  "zh-Hans": {
    "common.refresh": "刷新",
    "common.save": "保存",
    "common.cancel": "取消",
    "common.open": "打开",
    "common.close": "关闭",
    "common.search": "搜索",
    "common.settings": "设置",
    "common.loading": "正在加载",
    "common.ready": "就绪",
    "common.start": "启动",
    "common.stop": "停止",
    "common.status": "状态",
    "common.error": "错误",
    "common.reset": "重置",
    "moon.terrain": "地形",
    "moon.mission": "任务",
    "moon.timeline": "时间线",
    "moon.layers": "图层",
    "moon.operator": "操作员",
    "moon.telemetry": "遥测",
    "moon.details": "详情",
    "moon.live": "实时",
    "moon.lunar_operator": "月面操作台",
    "moon.globe_label": "可移动的来源可追溯月球仪",
    "moon.physical": "实体",
    "moon.lighting_time": "光照时间戳",
    "moon.focus_site": "聚焦站点",
    "moon.pause_orbit": "暂停环绕",
    "moon.resume_orbit": "继续环绕",
    "moon.evidence_authority": "证据权威性",
    "moon.current_blockers": "当前阻碍",
    "moon.evidence_dossier": "证据档案",
    "moon.elevation": "高程",
    "moon.slope": "坡度",
    "moon.roughness": "粗糙度",
    "moon.hazard": "危险",
    "moon.routes": "路线",
    "moon.evidence": "证据",
    "moon.power": "电力",
    "moon.selected_cell": "所选网格",
    "moon.blockers": "阻碍",
    "moon.route_preview": "路线预览",
    "moon.site_details": "站点详情",
    "moon.no_selection": "未选择",
    "moon.warning": "警告",
    "moon.site_terrain": "站点地形",
    "moon.first_square": "首个可信区域",
    "moon.selected_route": "任务所选路线",
    "moon.score": "评分",
    "moon.max_grade": "最大坡度",
    "moon.ranked_window": "排名最高的下一窗口",
    "moon.compare_routes": "比较候选路线",
    "moon.candidate": "候选",
    "moon.mission_selected": "已选择任务",
    "moon.site_score": "站点评分",
    "moon.blocked_routes": "受阻路线",
    "moon.energy_margin": "能量余量（瓦时）",
    "moon.motion_handoff": "运动交接",
    "moon.lunar_site_model": "月面站点模型",
    "moon.route_candidates": "候选路线",
    "moon.blocked": "受阻",
    "moon.confidence": "置信度",
    "moon.source": "来源",
    "moon.motion_contract": "运动契约",
    "moon.physics_core": "物理核心",
    "moon.motion_owner": "运动负责人",
    "moon.simulation": "仿真",
    "moon.facts": "事实",
    "moon.catalog_evidence": "目录证据",
    "moon.catalog_validation": "目录验证",
    "moon.evidence_loaded": "证据已加载",
    "moon.reset_view": "重置视图",
    "moon.mission_decision": "任务决策",
    "moon.next_action": "下一项必要操作",
    "moon.controlling_blockers": "控制性阻碍",
    "moon.illumination_evidence": "光照证据",
    "moon.illumination_block": "光照受阻",
    "moon.illumination_review": "光照待审",
    "moon.illumination_pass": "光照通过",
    "moon.current_route_window": "当前路线窗口",
    "moon.evidence_handoff": "证据与交接",
    "moon.not_viable": "不可行"
  }
};
const zhHans = {
  "Refresh": "刷新",
  "Save": "保存",
  "Cancel": "取消",
  "Open": "打开",
  "Close": "关闭",
  "Search": "搜索",
  "Settings": "设置",
  "Loading": "正在加载",
  "Ready": "就绪",
  "Start": "启动",
  "Stop": "停止",
  "Status": "状态",
  "Error": "错误",
  "Reset": "重置",
  "Terrain": "地形",
  "Mission": "任务",
  "Timeline": "时间线",
  "Layers": "图层",
  "Operator": "操作员",
  "Telemetry": "遥测",
  "Details": "详情",
  "Live": "实时",
  "Lunar operator": "月面操作台",
  "Movable source-backed Moon globe": "可移动的来源可追溯月球仪",
  "Physical": "实体",
  "Lighting timestamp": "光照时间戳",
  "Focus site": "聚焦站点",
  "Pause orbit": "暂停环绕",
  "Resume orbit": "继续环绕",
  "Evidence authority": "证据权威性",
  "Current blockers": "当前阻碍",
  "Evidence dossier": "证据档案",
  "Elevation": "高程",
  "Slope": "坡度",
  "Roughness": "粗糙度",
  "Hazard": "危险",
  "Routes": "路线",
  "Evidence": "证据",
  "Power": "电力",
  "Selected cell": "所选网格",
  "Blockers": "阻碍",
  "Route preview": "路线预览",
  "Site details": "站点详情",
  "No selection": "未选择",
  "Warning": "警告",
  "Site terrain": "站点地形",
  "First trusted square": "首个可信区域",
  "Mission selected route": "任务所选路线",
  "Score": "评分",
  "Max grade": "最大坡度",
  "Ranked next window": "排名最高的下一窗口",
  "Compare route candidates": "比较候选路线",
  "Candidate": "候选",
  "Mission selected": "已选择任务",
  "Site score": "站点评分",
  "Blocked routes": "受阻路线",
  "Energy margin Wh": "能量余量（瓦时）",
  "Motion handoff": "运动交接",
  "Lunar site model": "月面站点模型",
  "Route candidates": "候选路线",
  "Blocked": "受阻",
  "Confidence": "置信度",
  "Source": "来源",
  "Motion contract": "运动契约",
  "Physics core": "物理核心",
  "Motion owner": "运动负责人",
  "Simulation": "仿真",
  "Facts": "事实",
  "Catalog evidence": "目录证据",
  "Catalog validation": "目录验证",
  "Evidence loaded": "证据已加载",
  "Reset view": "重置视图",
  "Mission decision": "任务决策",
  "Next required action": "下一项必要操作",
  "Energy margin": "能量余量",
  "Controlling blockers": "控制性阻碍",
  "Illumination evidence": "光照证据",
  "current and ranked window": "当前与排名窗口",
  "illumination block": "光照受阻",
  "illumination review": "光照待审",
  "illumination pass": "光照通过",
  "Current route window": "当前路线窗口",
  "candidate": "候选",
  "not viable": "不可行",
  "Evidence and handoff": "证据与交接",
  "source authority and adapter boundary": "来源权威与适配器边界"
};
const zhHansTemplates = [
  { source: "{count} measured corridors", target: "{count} 条实测路线" },
];

const COOKIE = "moonsuite_interface_locale";
const STORAGE = "moonsuite.interfaceLocale";
const WINDOW_NAME_PREFIX = "moonsuite-locale:";
const VALID_PREFERENCES = new Set(["system", "en-US", "zh-Hans"]);

function normalizedPreference(value) {
  return VALID_PREFERENCES.has(value) ? value : null;
}

function windowNamePreference() {
  return window.name.startsWith(WINDOW_NAME_PREFIX)
    ? normalizedPreference(window.name.slice(WINDOW_NAME_PREFIX.length))
    : null;
}

function selectedPreference() {
  const query = normalizedPreference(new URLSearchParams(location.search).get("locale"));
  const cookie = document.cookie.match(new RegExp("(?:^|; )" + COOKIE + "=([^;]+)"));
  const storedCookie = cookie ? normalizedPreference(decodeURIComponent(cookie[1])) : null;
  return query || storedCookie || windowNamePreference() ||
    normalizedPreference(localStorage.getItem(STORAGE)) || "system";
}

function selectedLocale() {
  const preference = selectedPreference();
  const raw = preference === "system"
    ? navigator.languages?.[0] || navigator.language || "en-US"
    : preference;
  return /^(zh(?:-|_)(?:Hans|CN|SG)|zh$)/i.test(raw) ? "zh-Hans" : "en-US";
}

function storeLocale(locale) {
  locale = normalizedPreference(locale) || "system";
  document.cookie = COOKIE + "=" + encodeURIComponent(locale) + "; Path=/; Max-Age=31536000; SameSite=Lax";
  localStorage.setItem(STORAGE, locale);
  window.name = WINDOW_NAME_PREFIX + locale;
  location.reload();
}

function escapeRegex(value) {
  return value.replace(/[.*+?^\x24{}()|[\]\\]/g, "\\$&");
}

function compileTemplate({ source, target }) {
  const names = [];
  let cursor = 0;
  let pattern = "^";
  for (const match of source.matchAll(/\{([A-Za-z][A-Za-z0-9_]*)\}/g)) {
    pattern += escapeRegex(source.slice(cursor, match.index)) + "(.+?)";
    names.push(match[1]);
    cursor = match.index + match[0].length;
  }
  pattern += escapeRegex(source.slice(cursor)) + "$";
  return { regex: new RegExp(pattern), names, target };
}

const zhTemplatePatterns = zhHansTemplates.map(compileTemplate);

function formatMessage(message, params = {}) {
  return message.replace(/\{([A-Za-z][A-Za-z0-9_]*)\}/g, (match, name) =>
    Object.prototype.hasOwnProperty.call(params, name) ? String(params[name]) : match,
  );
}

function message(key, params = {}) {
  const locale = selectedLocale();
  const template = catalogs[locale]?.[key] ?? catalogs["en-US"]?.[key] ?? key;
  return formatMessage(template, params);
}

function elementParams(element) {
  try {
    return JSON.parse(element.getAttribute("data-i18n-params") || "{}");
  } catch {
    return {};
  }
}

function translateKeyedElement(element) {
  const key = element.getAttribute("data-i18n");
  if (key) {
    const translated = message(key, elementParams(element));
    if (element.textContent !== translated) element.textContent = translated;
  }
  for (const attribute of ["placeholder", "title", "aria-label"]) {
    const attributeKey = element.getAttribute("data-i18n-" + attribute);
    if (attributeKey) {
      const translated = message(attributeKey, elementParams(element));
      if (element.getAttribute(attribute) !== translated) element.setAttribute(attribute, translated);
    }
  }
}

function translateText(text) {
  const leading = text.match(/^\s*/)?.[0] || "";
  const trailing = text.match(/\s*$/)?.[0] || "";
  const core = text.slice(leading.length, text.length - trailing.length);
  if (core in zhHans) return leading + zhHans[core] + trailing;
  for (const template of zhTemplatePatterns) {
    const match = core.match(template.regex);
    if (!match) continue;
    const params = Object.fromEntries(template.names.map((name, index) => [name, match[index + 1]]));
    return leading + formatMessage(template.target, params) + trailing;
  }
  return text;
}

function translate(root) {
  if (root.closest?.('[data-i18n-scope="content"],[data-i18n-scope="technical"],[translate="no"]')) return;
  for (const element of root.querySelectorAll?.("[data-i18n],[data-i18n-placeholder],[data-i18n-title],[data-i18n-aria-label]") || []) {
    if (!element.closest('[data-i18n-scope="content"],[data-i18n-scope="technical"],[translate="no"]')) {
      translateKeyedElement(element);
    }
  }
  if (root.matches?.("[data-i18n],[data-i18n-placeholder],[data-i18n-title],[data-i18n-aria-label]")) {
    translateKeyedElement(root);
  }
  if (selectedLocale() !== "zh-Hans") return;
  const walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT, {
    acceptNode(node) {
      return /\S/.test(node.nodeValue || "") &&
        !["SCRIPT", "STYLE", "CODE", "PRE", "TEXTAREA"].includes(node.parentElement?.tagName) &&
        !node.parentElement?.closest('[data-i18n-scope="content"],[data-i18n-scope="technical"],[translate="no"]') &&
        !node.parentElement?.closest("[data-i18n]")
        ? NodeFilter.FILTER_ACCEPT
        : NodeFilter.FILTER_REJECT;
    },
  });
  const nodes = [];
  while (walker.nextNode()) nodes.push(walker.currentNode);
  for (const node of nodes) {
    const translated = translateText(node.nodeValue || "");
    if (node.nodeValue !== translated) node.nodeValue = translated;
  }
  for (const element of root.querySelectorAll?.("[placeholder],[title],[aria-label]") || []) {
    for (const attribute of ["placeholder", "title", "aria-label"]) {
      if (element.hasAttribute(attribute)) {
        const translated = translateText(element.getAttribute(attribute) || "");
        if (element.getAttribute(attribute) !== translated) element.setAttribute(attribute, translated);
      }
    }
  }
  if (root.matches?.("[placeholder],[title],[aria-label]")) {
    for (const attribute of ["placeholder", "title", "aria-label"]) {
      if (root.hasAttribute(attribute)) {
        const translated = translateText(root.getAttribute(attribute) || "");
        if (root.getAttribute(attribute) !== translated) root.setAttribute(attribute, translated);
      }
    }
  }
}

function installSelector() {
  if (document.getElementById("moonsuite-language")) return;
  const label = document.createElement("label");
  label.id = "moonsuite-language";
  const languageLabel = selectedLocale() === "zh-Hans" ? "界面语言" : "Interface language";
  label.setAttribute("aria-label", languageLabel);
  Object.assign(label.style, {
    position: "fixed", right: "14px", bottom: "14px", zIndex: "2147483647",
    padding: "6px", border: "1px solid rgba(148,163,184,.45)", borderRadius: "10px",
    background: "rgba(255,255,255,.94)", boxShadow: "0 8px 28px rgba(15,23,42,.16)",
  });
  const select = document.createElement("select");
  select.setAttribute("aria-label", languageLabel);
  select.style.cssText = "border:0;background:transparent;color:#0f172a;font:600 13px system-ui;cursor:pointer";
  select.innerHTML = '<option value="system">System / 跟随系统</option><option value="en-US">English</option><option value="zh-Hans">简体中文</option>';
  select.value = selectedPreference();
  select.addEventListener("change", () => storeLocale(select.value));
  label.append(select);
  document.body.append(label);
}

function boot() {
  const locale = selectedLocale();
  window.name = WINDOW_NAME_PREFIX + selectedPreference();
  document.documentElement.lang = locale;
  document.documentElement.dir = "ltr";
  if (locale === "zh-Hans") {
    document.body.style.fontFamily = "'PingFang SC','Microsoft YaHei','Noto Sans CJK SC',system-ui,sans-serif";
  }
  translate(document.body);
  installSelector();
  new MutationObserver((records) => {
    for (const record of records) {
      if (record.type === "childList") for (const node of record.addedNodes) {
        if (node.nodeType === Node.ELEMENT_NODE) translate(node);
        else if (node.nodeType === Node.TEXT_NODE && node.parentElement) translate(node.parentElement);
      }
      if (record.type === "characterData" && record.target.parentElement) {
        translate(record.target.parentElement);
      }
      if (record.type === "attributes" && record.target.nodeType === Node.ELEMENT_NODE) {
        translate(record.target);
      }
    }
  }).observe(document.body, {
    childList: true,
    characterData: true,
    attributes: true,
    attributeFilter: ["placeholder", "title", "aria-label", "data-i18n", "data-i18n-params"],
    subtree: true,
  });
}

window.MoonSuiteI18n = Object.freeze({
  locale: selectedLocale,
  preference: selectedPreference,
  message,
  translate,
});

if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot);
else boot();

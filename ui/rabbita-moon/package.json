{
  "name": "moonmoon-rabbita-moon",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "moon build --target js --debug && node prepare-rabbita-build.mjs debug",
    "dev": "vite --host 127.0.0.1 --port 8766",
    "prebuild": "moon build --target js --release && node prepare-rabbita-build.mjs release",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8766"
  },
  "dependencies": {
    "@rabbita/vite": "^0.2.0",
    "vite": "^7.0.0"
  }
}

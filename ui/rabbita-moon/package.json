{
  "name": "moonmoon-rabbita-moon",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:live-moonrobo": "node prepare-live-moonrobo-clip.mjs",
    "prepare:e1-asm": "node prepare-e1-asm-assets.mjs",
    "predev": "npm run prepare:live-moonrobo && npm run prepare:e1-asm && moon build --target js --debug && node prepare-rabbita-build.mjs debug",
    "dev": "vite --host 127.0.0.1 --port 8766",
    "prebuild": "npm run prepare:live-moonrobo && npm run prepare:e1-asm && moon build --target js --release && node prepare-rabbita-build.mjs release",
    "build": "vite build",
    "export:moonrobo-contract": "node export-moonrobo-contract.mjs",
    "export:gait-evidence": "npm run prepare:live-moonrobo && node export-rabbita-gait-evidence.mjs",
    "check:live-suite-payload": "node check-live-suite-payload.mjs",
    "check:live-moonrobo-suite": "npm run prepare:live-moonrobo && node check-live-moonrobo-suite.mjs",
    "check:gait": "npm run prepare:e1-asm && node check-rabbita-gait-contract.mjs",
    "check:gait:heavy": "npm run prepare:live-moonrobo && npm run prepare:e1-asm && node check-rabbita-gait-contract.mjs --heavy",
    "preview": "vite preview --host 127.0.0.1 --port 8766"
  },
  "dependencies": {
    "@rabbita/vite": "^0.2.0",
    "three": "^0.185.0",
    "vite": "^7.0.0"
  }
}

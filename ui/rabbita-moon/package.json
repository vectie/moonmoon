{
  "name": "moonmoon-rabbita-moon",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:live-moonrobo": "node prepare-live-moonrobo-clip.mjs",
    "prepare:e1-asm": "node prepare-e1-asm-assets.mjs",
    "prepare:lola-terrain": "node prepare-lola-terrain-asset.mjs",
    "predev": "npm run prepare:e1-asm && npm run prepare:live-moonrobo && moon build --target js --debug && node prepare-rabbita-build.mjs debug",
    "dev": "vite --host 127.0.0.1 --port 8766",
    "prebuild": "npm run prepare:e1-asm && npm run prepare:live-moonrobo && moon build --target js --release && node prepare-rabbita-build.mjs release",
    "build": "vite build",
    "export:moonrobo-contract": "npm run prepare:e1-asm && node export-moonrobo-contract.mjs",
    "export:gait-evidence": "npm run prepare:e1-asm && npm run prepare:live-moonrobo && node export-rabbita-gait-evidence.mjs",
    "check:live-suite-payload": "node check-live-suite-payload.mjs",
    "check:live-moonrobo-suite": "npm run prepare:e1-asm && npm run prepare:live-moonrobo && node check-live-moonrobo-suite.mjs",
    "check:gait": "npm run prepare:e1-asm && node check-rabbita-gait-contract.mjs",
    "check:gait:heavy": "npm run prepare:e1-asm && npm run prepare:live-moonrobo && node check-rabbita-gait-contract.mjs --heavy",
    "preview": "vite preview --host 127.0.0.1 --port 8766"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/inter": "^5.2.8",
    "@rabbita/vite": "^0.2.0",
    "lucide": "^1.24.0",
    "three": "^0.185.0",
    "vite": "^7.0.0"
  }
}
